{
  "name": "cypress-automation-framework",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:open:test": "CYPRESS_ENV=test cypress open",
    "cy:run:test": "CYPRESS_ENV=test cypress run",
    "cy:open:staging": "CYPRESS_ENV=staging cypress open",
    "cy:run:staging": "CYPRESS_ENV=staging cypress run",
    "cy:open:dev": "CYPRESS_ENV=dev cypress open",
    "cy:run:dev": "CYPRESS_ENV=dev cypress run",
    "cy:open:production": "CYPRESS_ENV=production cypress open",
    "cy:run:production": "CYPRESS_ENV=production cypress run",
    "test": "npm run cy:run"
  },
  "devDependencies": {
    "cypress": "^12.0.0"
  }
}
